% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_data}
\alias{load_data}
\title{Load Global Measles Case Data}
\usage{
load_data(dataset = "month")
}
\arguments{
\item{dataset}{Character string specifying which dataset to load.
One of \code{"month"} (default), \code{"year"}, or \code{"both"}. When \code{"both"} is
chosen, a named list with elements \code{cases_month} and \code{cases_year} is
returned.}
}
\value{
When \code{dataset} is \code{"month"} or \code{"year"}, a tibble. When
\code{dataset} is \code{"both"}, a named list of two tibbles:
\describe{
\item{cases_month}{Monthly case counts per country. Key columns:
\code{country}, \code{year}, \code{month}, \code{measles_total}.}
\item{cases_year}{Annual case counts per country. Key columns:
\code{country}, \code{year}, \code{measles_total}, \code{measles_clinical},
\code{measles_epi_linked}, \code{measles_lab_confirmed},
\code{total_population}.}
}
}
\description{
Downloads and returns one or both of the WHO measles datasets from the
Tidy Tuesday GitHub repository (2025-06-24 release). Two datasets are
available: monthly case counts (\code{"month"}) and annual case counts
(\code{"year"}).
}
\examples{
\dontrun{
  # Monthly data only (default)
  cases_month <- load_data()

  # Annual data
  cases_year <- load_data("year")

  # Both at once
  dat <- load_data("both")
  cases_month <- dat$cases_month
  cases_year  <- dat$cases_year
}

}
